# Image Cache Configuration
# When set, these override the corresponding UI settings and lock the fields.

# IMAGE_CACHE_DIR=.cache/img
# IMAGE_CACHE_MAX_SIZE_MB=500
# IMAGE_CACHE_TTL_DAYS=7

# Media Cache Configuration (audio files: songs, podcasts, etc.)
# When set, these override the corresponding UI settings and lock the fields.

# MEDIA_CACHE_DIR=.cache/media
# MEDIA_CACHE_MAX_SIZE_MB=2048
# MEDIA_CACHE_TTL_DAYS=30
